# Qwen3-VL reranker service - pinned deps. Install in a fresh conda env
# (python 3.10), separate from the kg4vd / GME env:
#
#   conda create -n kg4vd_reranker python=3.10 -y
#   conda activate kg4vd_reranker
#   pip install torch==2.8.0 torchvision==0.23.0 \
#       --index-url <your-pytorch-wheel-index>
#   pip install -r services/reranker/requirements.txt
#
# torch + torchvision MUST be installed first from the PyTorch CUDA wheel
# index. Choose a wheel index compatible with your driver and CUDA runtime.

transformers>=4.57.0
qwen-vl-utils>=0.0.14
sentence-transformers>=5.0.0
accelerate>=1.0.0
fastapi>=0.110.0
uvicorn>=0.30.0
pillow>=10.0.0
pydantic>=2.5.0
httpx>=0.27.0
